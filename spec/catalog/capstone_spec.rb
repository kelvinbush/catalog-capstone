# frozen_string_literal: true

RSpec.describe Catalog::Capstone do
  it "has a version number" do
    expect(Catalog::Capstone::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(false)
  end
end
